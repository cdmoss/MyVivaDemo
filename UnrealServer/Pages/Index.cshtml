@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Recent activity:</h1>
    @foreach (var pin in Model.Pins)
    {
        <p>@pin.TimeStamp: New pin dropped</p>
    }
</div>
